[package]
name = "flatvodka"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5", features = ["derive"] }

rust-ini = "0.21"

walkdir = "2.5"

nix = { version = "0.29", features = ["user", "process", "fs"] }

libc = "0.2"
